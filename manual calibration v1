#define NUM_SENSORS 8

int sensorPins[NUM_SENSORS] = {A0, A1, A2, A3, A4, A5, A6, A7};

void setup() {
  Serial.begin(9600);
}

void loop() {

  for (int i = 0; i < NUM_SENSORS; i++) {
    int value = analogRead(sensorPins[i]);

    Serial.print(value);
    Serial.print(" ");
  }

  Serial.println(); // new line after 8 values

  delay(200);
}
